apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dws-ticket-service.fullname" . }}-test-connection"
  labels:
    {{- include "dws-ticket-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dws-ticket-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
