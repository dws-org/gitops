apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dws-event-service.fullname" . }}-test-connection"
  labels:
    {{- include "dws-event-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dws-event-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
